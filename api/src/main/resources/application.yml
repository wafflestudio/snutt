spring:
  config:
    import: application-common.yml

  jackson:
    property-naming-strategy: SNAKE_CASE

snutt:
  secret-key:

---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri:
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri:
