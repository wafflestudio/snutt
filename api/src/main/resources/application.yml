spring:
  config:
    import: application-common.yml

snutt:
  secret-key:

---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri:
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri:
