spring:
  config:
    import: application-common.yml
  jackson:
    default-property-inclusion: non_null

snutt:
  secret-key:

server:
  netty:
    connection-timeout: 5s
    idle-timeout: 5s

---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri:
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri:

logging:
  level:
    reactor.netty: OFF
