spring:
  profiles:
    active: local
  data:
    mongodb:
      uri:
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

secret-names: dev/snutt-timetable

fcm:
  project-id: snutt-1470373443173
  secret-names: dev/snutt-timetable/google-service-account
---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri:
      auto-index-creation: true
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri:
fcm:
  project-id: snutt-792f1
  secret-names: prod/snutt-timetable/google-service-account

secret-names: prod/snutt-timetable
