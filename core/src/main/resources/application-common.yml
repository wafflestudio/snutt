spring:
  profiles:
    active: local
  data:
    mongodb:
      uri:
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
google:
  firebase:
    project-id:
    service-account:

secret-names: dev/snutt-timetable

---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri:
      auto-index-creation: true
google:
  firebase:
    project-id:
    service-account:
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri:
google:
  firebase:
    project-id:
    service-account:

secret-names: prod/snutt-timetable
